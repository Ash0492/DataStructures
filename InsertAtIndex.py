def inserAtIndex(arr, sizeOfArray, index, element):
        arr.insert(index,element)
        print(arr)

arr = [1, 2, 3, 4, 5]
sizeOfArray = 6
index = 2
element = 90

inserAtIndex(arr, sizeOfArray, index, element)